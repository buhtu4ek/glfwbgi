// Install HomeBrew
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

// install glfw
brew install glfw3

// Command for shell scripts
chmod +x <filename>

// Creating static lib
ar rcs libglfwbgi.a glfwbgi.o

// Building test app
g++ -std=c++11 -stdlib=libc++ -lglfw -framework CoreVideo -framework OpenGL -framework IOKit -framework Cocoa -framework Carbon glfwtest.cpp -o glfwtest